# Example workbook directory

The tracked workbook in this directory is an audited example used for software validation and demonstrations. Other `.xlsx` / `.xlsm` files placed here are ignored by Git by default so researchers do not accidentally commit private study data.

See `../docs/EXAMPLE_DATA.md` for the publication/data-use note.
